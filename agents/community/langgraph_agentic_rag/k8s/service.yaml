apiVersion: v1
kind: Service
metadata:
  name: langgraph_agentic_rag
  labels:
    app: langgraph_agentic_rag
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: langgraph_agentic_rag